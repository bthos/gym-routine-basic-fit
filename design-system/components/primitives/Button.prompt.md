Basic-Fit button: rectangular (4px radius), bold UPPERCASE label, purple action color (`--bf-purple`) — matches "EMPEZAR" / "VER PRECIOS" on basic-fit.com. Orange is a brand/surface color, never a button color.

```jsx
<Button variant="primary">empezar</Button>
<Button variant="outline">ver precios</Button>
```

Variants: primary (solid purple), outline (white bg, purple border + text), secondary (solid ink), ghost (purple text only), inverse (white, for dark/orange surfaces). Sizes sm/md/lg. Labels auto-uppercase via CSS.
