Alternating photo + text band ("¿Por qué Basic-Fit?"): rounded photo, uppercase H2, body copy, optional check list + CTA. `reverse` flips sides; `tone="ink"` renders the dark band.

```jsx
<FeatureSplit
  title="¿Por qué Basic-Fit?"
  checks={["Más de 1600 clubes", "Abierto 24/7", "Mejor calidad-precio"]}
  imageUrl="…" cta={{ label: "saber más" }}
/>
```
