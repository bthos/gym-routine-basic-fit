Dark site footer (ink) with logo, optional link columns ("Servicios e información", "Gimnasios"…) and small legal lines above a hairline.

```jsx
<PageFooter
  columns={[{ title: "Gimnasios", links: [{ label: "Madrid" }, { label: "Málaga" }] }]}
  lines={["Programa personal — no sustituye asesoramiento médico"]}
/>
```
